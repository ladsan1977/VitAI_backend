# VitAI Backend - Production Environment Configuration
# Copy this file and set actual values in Render Dashboard → Environment

# =============================================================================
# Application Configuration
# =============================================================================
APP_ENV=production
LOG_LEVEL=INFO
PORT=8000

# =============================================================================
# OpenAI API Configuration
# =============================================================================
# Get your API key from: https://platform.openai.com/api-keys
OPEN_AI_KEY=sk-proj-your-actual-openai-key-here
OPENAI_MODEL=gpt-5.1-chat-latest
OPENAI_MAX_OUTPUT_TOKENS=4000
OPENAI_TEMPERATURE=0.1

# =============================================================================
# Security Configuration
# =============================================================================
# Generate a secure API key using:
# python -c "import secrets; print(f'vitai_sk_prod_{secrets.token_urlsafe(32)}')"
API_KEY=vitai_sk_prod_REPLACE_WITH_SECURE_RANDOM_STRING
API_KEY_HEADER=X-API-Key

# =============================================================================
# Rate Limiting Configuration
# =============================================================================
# Adjust based on usage and OpenAI costs
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=10
RATE_LIMIT_PER_HOUR=100

# =============================================================================
# CORS Configuration
# =============================================================================
# UPDATE WITH YOUR ACTUAL FRONTEND DOMAIN(S)
# Format: JSON array of allowed origins
CORS_ORIGINS=["https://your-frontend-domain.com","https://www.your-domain.com"]

# =============================================================================
# Production Security
# =============================================================================
HTTPS_ONLY=true
ALLOWED_HOSTS=["*.onrender.com","your-custom-domain.com"]

# =============================================================================
# Database & Redis (NOT NEEDED - App is stateless!)
# =============================================================================
# The application is completely stateless and only calls the OpenAI API.
# No database or Redis configuration required.
# Rate limiting uses in-memory storage (resets on restart/sleep).

# =============================================================================
# Deployment Notes
# =============================================================================
# 1. Set these variables in Render Dashboard → Your Service → Environment
# 2. Never commit actual secrets to git
# 3. Render auto-provides PORT environment variable
# 4. Update CORS_ORIGINS after frontend deployment
# 5. Monitor OpenAI usage at platform.openai.com/usage
